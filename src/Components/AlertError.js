import React from "react";

const AlertError = () => {
  return (
    <div class="alert alert-danger" role="alert">
      Verify the medias -- Check it out !
    </div>
  );
};

export default AlertError;
